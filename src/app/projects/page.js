import ProjectsClient from "./ProjectsClient";

export const metadata = {
    title: "Projects",
    description:
        "Explore the solar plants and renewable projects we manage across India.",
};

export default function ProjectsPage() {
    return <ProjectsClient />;
}